<VirtualHost *:80>
    DocumentRoot /var/www/html/public

    <Directory /var/www/html/public>
        AllowOverride All
        Require all granted
        Options -Indexes
    </Directory>

    # Logs dans la sortie standard Docker
    ErrorLog /dev/stderr
    CustomLog /dev/stdout combined
</VirtualHost>